let hello = 'Hola perro'
let world = 'Mundo'

let epicPhrase = hello + ' ' + world
console.log(epicPhrase)

// template literals
let epicPhrase2 = `${hello} ${world}`
console.log(epicPhrase2 + ' ' + epicPhrase2.length)